# Managed by chezmoi

# Common
source ~/.config/zsh/common.zsh

# Host/VM specific
if [[ "$(hostname -s)" == "virtualmachine" ]]; then
  source ~/.config/zsh/vm.zsh
else
  source ~/.config/zsh/host.zsh
fi
